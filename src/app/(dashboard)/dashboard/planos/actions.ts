"use server";
import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
export async function requestPlan(formData: FormData) { const supabase=createClient(); const {data,error}=await supabase.rpc("create_subscription_payment",{p_plan_id:String(formData.get("plan_id")),p_billing_cycle:String(formData.get("billing_cycle")) as "monthly"|"yearly",p_payment_method:String(formData.get("payment_method")) as "mpesa"|"emola"|"mkesh"|"bank_transfer"|"cash"|"other",p_transaction_id:String(formData.get("transaction_id")??"").trim()||null,p_notes:String(formData.get("notes")??"").trim()||null}); if(error){console.error("create_subscription_payment",error);const message=error.message.includes("transação já foi")?"Esta transação já foi utilizada.":"Não foi possível criar o pedido de pagamento.";redirect(`/dashboard/planos?erro=${encodeURIComponent(message)}`)} redirect(`/dashboard/pagamentos?criado=${data.id}`); }
